""" common base classes

    describable:

        exposes a "describe()" method that returns
        a description of the game object 

    flaggable:

        the object can store flags, and exposes a
        "get_flags()" method that returns the flags of itself
        and its parent object tree

    stattable:

        the object has a dict of stats

"""

